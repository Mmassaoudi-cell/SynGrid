# ============================================
# SynGrid v4.0 — Dependencies
# ============================================

# Core dependencies (GUI + Bridge)
matplotlib>=3.5.0
numpy>=1.21.0
Pillow>=9.0.0
pypower>=5.1.4

# Optional dependencies for the backend engine (Real_SW.py)
# The application runs without these — graceful fallbacks are built in.
networkx>=3.0
websockets>=11.0.0
cryptography>=41.0.0
psutil>=5.9.0
